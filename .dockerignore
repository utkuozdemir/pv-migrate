# Keep the build context small and free of local state. Every stage copies the
# paths it needs explicitly, so nothing here is required by a build; this is
# about not shipping the working tree to the daemon and not invalidating caches
# on unrelated local churn. .git in particular changes on every commit.
.git
_work
dist
.idea
.vscode
.DS_Store
*.patch
*.diff
